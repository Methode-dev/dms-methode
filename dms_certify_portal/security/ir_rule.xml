<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!--
            There is deliberately NO access rule granting base.group_public or
            base.group_portal any right on dms.certificate.

            The public user must reach these records through the controller and
            nowhere else. If you ever find yourself adding a public ACL line to
            make something work, the fix is in the controller, not here: an ACL
            would also open /web/dataset/call_kw, the ORM RPC endpoint and the
            website search, all of which bypass your rate limit and your
            passport check.
        -->

        <!--
            Scoped on the certificate's own company, not on its creator's.
            Every certificate is written under sudo (the passport fields are
            group-restricted), so a rule reading create_uid would file the whole
            registry under OdooBot.
        -->
        <record id="rule_dms_certificate_company" model="ir.rule">
            <field name="name">Certified documents: own company</field>
            <field name="model_id" ref="model_dms_certificate"/>
            <field name="domain_force">[('company_id', 'in', company_ids)]</field>
            <field name="groups" eval="[Command.link(ref('group_certify_user'))]"/>
        </record>

    </data>
</odoo>
